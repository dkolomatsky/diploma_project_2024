// import { contentContainer } from "./cards.js";

// let cartPrice = 0;

// export const getDataPrice = (target) => {
//   return +target.parentElement.previousElementSibling.innerHTML.replace(
//     // в регулрке используем карманы чтобы получаь доступ отдельно до каждого числа (валюта и копейки например)
//     /^\$(\d+)\s\D+(\d+).*$/,
//     "$1.$2"
//   );
// };
// export const getPrice = () => {
//   return Math.round((cartPrice + totalPrice) * 100) / 100;
// };
